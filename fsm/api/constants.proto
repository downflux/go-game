// constants.proto
//
// Constants that are useful for finite state automata.
syntax = "proto3";

package game.fsm.api.constants;
option go_package = "game.fsm.api.constants";
option csharp_namespace = "DF.Game.FSM.API.Constants";

enum FSMType {
  FSM_TYPE_UNKNOWN = 0;
  FSM_TYPE_MOVE = 1;
}
